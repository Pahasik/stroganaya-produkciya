# Сайт производителя сухой строганой продукции

Одностраничный B2B-сайт для сбора оптовых заявок: клеёная имитация бруса, клеёный планкен,
калиброванная и другая строганая продукция.

Стек: **Vite 8 + React 19 + TypeScript + Tailwind CSS v4**. Шрифты (Manrope, Inter)
подключены локально через `@fontsource-variable` — внешних запросов у сайта нет.

## Запуск

```bash
npm install
npm run dev
```

Сайт откроется на `http://localhost:5173` (или на указанном порту).

| Команда | Что делает |
| --- | --- |
| `npm run dev` | Локальная разработка |
| `npm run build` | Проверка типов + production-сборка в `dist/` |
| `npm run preview` | Просмотр собранной версии |
| `npm run lint` | Линтер (oxlint) |
| `npm run images` | Пересборка WebP из `raw-images/` в `public/images/` |

## Публикация на GitHub Pages

Деплой настроен в `.github/workflows/deploy.yml` и запускается автоматически
при каждом пуше в ветку `main`.

Базовый путь берётся из имени репозитория (`VITE_BASE=/<имя-репозитория>/`),
поэтому репозиторий можно переименовать — ничего чинить не придётся.
Локально `base` остаётся `/`, разработке это не мешает.

Разовая настройка после создания репозитория:
**Settings → Pages → Build and deployment → Source: GitHub Actions.**

Сайт будет доступен по адресу `https://<логин>.github.io/<имя-репозитория>/`.

## Что нужно заменить перед публикацией

Все места помечены в коде комментарием `TODO(владелец сайта)`.

| Файл | Что заменить |
| --- | --- |
| `src/data/content.ts` → `contacts.max` | Ссылка на профиль в мессенджере MAX. Сейчас рядом с телефоном просто сказано, что в MAX можно написать на тот же номер |
| `src/data/content.ts` → `contacts.telegram` | Проверить ссылку на Telegram. Сейчас это ссылка по номеру `t.me/+79200044533`; если есть @username, лучше поставить его |
| `index.html` | Домен в `canonical`, `og:url`, `og:image` — сейчас адрес превью на GitHub Pages |
| `src/components/PrivacyDialog.tsx` | Полное юридическое наименование, ИНН и ОГРН оператора персональных данных |
| `public/images/` | Заменить изображения на реальные фотографии производства, затем `npm run images` |

## Форма заявки

Обязательны только **имя и телефон** — остальное менеджер уточняет при звонке.
Галка согласия проставлена заранее, посетитель может её снять.

Заявки уходят письмом на **sudik-les@mail.ru** через сервис FormSubmit.

> **Разовая активация.** После первой отправки формы с боевого домена FormSubmit
> пришлёт на sudik-les@mail.ru письмо со ссылкой активации. Пока по ней не перешли,
> письма с заявками не доставляются. Проверьте папку «Спам».

Что уже сделано против типичных проблем:

- отправка идёт методом `POST`, а не `GET`;
- у формы задан `action`, поэтому при отказе JavaScript данные уйдут обработчику,
  а не попадут в адресную строку;
- защита от повторной отправки: кнопка блокируется, параллельный запрос отсекается;
- две antispam-ловушки: скрытое поле `_honey` и отсечка мгновенной отправки (< 2,5 с);
- состояния «отправка», «успех» и «ошибка» с текстом ошибки и телефоном для связи.

**Как сменить сервис.** Задайте `VITE_LEAD_ENDPOINT` в `.env.local` — он имеет приоритет
над FormSubmit. Обработчик должен принимать `POST` с JSON-телом `Lead`
(см. `src/lib/submitLead.ts`) и отвечать кодом 2xx. Токены и ключи в код фронтенда
класть нельзя — он публичный.

## Изображения

Изображения сгенерированы под сюжеты сайта: общий вид производства, оборудование
в работе, торец и профиль имитации бруса, профиль планкена, поверхность крупным планом,
замер сечения, упаковка, пачки продукции, склад, погрузка и транспорт.

Это **не документальные снимки конкретного производства**. Чужие фотографии не
использовались — публикация снимков из интернета нарушила бы авторские права.
На изображениях нет чужих логотипов, товарных знаков, узнаваемых лиц, номерных
знаков и посторонних надписей.

Исходники лежат в `raw-images/`, оптимизированные WebP нескольких размеров —
в `public/images/`. После замены исходников запустите `npm run images`:
скрипт пересоберёт WebP, размытые заглушки и `src/data/image-manifest.json`.

Перед публикацией имеет смысл заменить их на реальные фотографии производства.

## Структура

```
src/
  components/        секции страницы
    ui/              переиспользуемые примитивы
      Picture.tsx        адаптивное изображение (WebP, srcset, lazy, LQIP)
      ProfileDrawing.tsx технические чертежи сечения продукции
      Reveal.tsx         появление блоков при прокрутке
      icons.tsx          линейные иконки
      Logo.tsx           знак и логотип «Костромской Профиль»
  MobileCta.tsx      закреплённая панель «Позвонить / Получить расчёт» на мобильных
  data/content.ts    ВЕСЬ текст и данные сайта — правки контента здесь
  lib/submitLead.ts  отправка заявки, маска и валидация телефона
  index.css          дизайн-токены (цвета, шрифты) и базовые стили
```

Контент отделён от вёрстки: цены, характеристики, преимущества, вопросы FAQ и шаги
заказа лежат в `src/data/content.ts`.

## Проверено

- Сборка и типы: `npm run build` — без ошибок; линтер — без замечаний.
- Адаптивность 360 / 768 / 1024 / 1440 px, горизонтального скролла нет
  (проверено обходом DOM, а не только `overflow: hidden`).
- Один `h1`, логичная иерархия заголовков, `alt` у всех изображений.
- Все якорные ссылки ведут к существующим секциям.
- Консоль без ошибок и предупреждений.
- Форма: пустая отправка, некорректный телефон, POST-разметка, защита от дублей, успешный сценарий.
- Клавиатурная навигация, видимые focus-состояния, `prefers-reduced-motion`.
